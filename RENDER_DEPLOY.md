# Triển khai lên Render

File `render.yaml` ở thư mục gốc là cấu hình triển khai bằng gói miễn phí:

- Giao diện React: Render Static Site.
- API Spring Boot: Render Free Web Service.
- Cơ sở dữ liệu: Aiven for MySQL Free.

Xem hướng dẫn [AIVEN_RENDER_FREE.md](AIVEN_RENDER_FREE.md) để biết từng bước tạo Aiven và các biến môi trường cần điền trên Render.

File `render-paid.yaml` được giữ lại để dùng sau này nếu cần triển khai ổn định hơn: MySQL và nơi lưu ảnh sẽ được đặt trực tiếp trên Render.
