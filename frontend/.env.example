# Local development (already the default when this file is absent)
VITE_API_URL=http://localhost:8081/api

# If frontend and backend are deployed on different domains, set this to the
# public HTTPS address of the backend before running the production build.
# VITE_API_URL=https://api.example.com/api
